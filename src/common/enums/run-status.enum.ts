export enum RunStatus {
  PENDING = 'pending',
  FETCHING = 'fetching',
  PROCESSING = 'processing',
  CLUSTERING = 'clustering',
  GENERATING = 'generating',
  COMPLETED = 'completed',
  FAILED = 'failed',
}

